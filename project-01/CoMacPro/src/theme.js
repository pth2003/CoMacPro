import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      light: "#FFEFD5",
      main: "#FFC900",
      dark: "#D0A300",
    },
    secondary: {
      light: "#FFD7BB",
      main: "#FFA21A",
      dark: "#CC8000",
    },
  },
});

export default theme;
